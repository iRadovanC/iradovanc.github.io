const sharp=require('C:/Users/Radovan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/sharp');
(async()=>{for(const f of ['soldiers-cutout.png','pm-cutout.png']){const {data,info}=await sharp('assets/'+f).raw().toBuffer({resolveWithObject:true});console.log(f,info);const alpha=Array.from({length:256},()=>0);for(let i=3;i<data.length;i+=4)alpha[data[i]]++;console.log('alpha',alpha.map((n,i)=>[i,n]).filter(([i,n])=>n>2000));if(f.startsWith('soldiers')){const rows=[];for(let y=0;y<info.height;y++){let count=0;for(let x=0;x<info.width;x++)if(data[(y*info.width+x)*4+3]>230)count++;rows.push(count)}for(let i=1;i<5;i++){const center=Math.round(info.height*i/5);console.log('boundary',i,rows.map((n,y)=>[y,n]).slice(center-55,center+55).sort((a,b)=>a[1]-b[1]).slice(0,8))}}}})()
